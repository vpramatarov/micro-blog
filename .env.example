GO_ENV=dev # dev, test, prod
PORT=8080
DB_STRING='vault.db'
ADMIN_SEED_PASSWORD = 'changeme'
JWT_SECRET='test-secret-do-not-use-in-prod'
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=168h
COOKIE_SECURE=false # Set to `false` for local non-HTTPS dev so the refresh cookie is actually sent.